# Core Python dependencies for embedding pipeline
# Install with: pip install -r requirements.txt

# PyTorch and related
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ImageBind dependencies (will be installed from git in setup.sh)
numpy<2.0
timm>=0.9.0
ftfy>=6.1.0
regex>=2022.0.0
einops>=0.6.0
iopath>=0.1.9

# Transformers for BLIP model
transformers>=4.35.2
huggingface_hub>=0.19.4

# Video processing
opencv-python>=4.8.0
scenedetect[opencv]>=0.6.2

# Audio processing
pydub>=0.25.1

# Vector database
pinecone>=3.0.0

# Image processing
pillow>=9.0.0

# HTTP requests for downloading from URLs
requests>=2.31.0

# Utilities
typing-extensions>=4.5.0
setuptools>=69.5.1

# Optional: Whisper (not used in pipeline but may be needed for other parts)
# openai-whisper>=20230918
